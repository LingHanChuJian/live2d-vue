module.exports = {
    baseUrl: '/',
    outputDir: 'dist',
    lintOnSave: true,
    productionSourceMap: false,
    css: {
        sourceMap: false,
        extract: false, // 不构建单独的css文件
        modules: false
    }
}